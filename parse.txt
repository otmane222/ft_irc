
---| pass 
	ERR_PASSWDMISMATCH    | password was either incorrect.
	ERR_NEEDMOREPARAMS    | there's not enough param.
	ERR_ALREADYREGISTERED | when a client tries to change a detail that can only be set during registration .

---| nick
	ERR_NONICKNAMEGIVEN   | when a nickname parameter is expected for a command but isn’t given.
	ERR_ERRONEUSNICKNAME  | nickname contains characters that are disallowed by the server.
	ERR_NICKNAMEINUSE 	  | nickname is already in use on the network.
	ERR_NICKCOLLISION 	  | nickname registered of a NICK that already exists by another server.
	
---| user
	ERR_NEEDMOREPARAMS    | there's not enough param.
	ERR_ALREADYREGISTERED | when a client tries to change a detail that can only be set during registration .

---| ping
	ERR_NEEDMOREPARAMS    | there's not enough param.
	ERR_NOSUCHSERVER      | that the given server name does not exist

---| join
	RPL_TOPIC             | Sent to a client when joining the <channel> to inform them of the current topic of the channel.
	RPL_TOPICWHOTIME      | Sent to a client to let them know who set the topic.
	RPL_ENDOFNAMES        | RPL_ENDOFNAMES(hostname, nick, channelName);
	RPL_NAMREPLY          | RPL_NAMREPLY(hostname, client, channelName, nick)